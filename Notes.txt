- Fix game resolution to 320x240x8 so we can use Mode-X in the DOS version
- No need to set palette of backbuffer and auxiliary surfaces: only the palette
    of the primary surface is used if we dont switch

Configure nvim
==============

Create .nvim.lua file with contents:

vim.o.makeprg = 'cmake --build build_win95 --target testDdraw/test2'
vim.keymap.set('n', '<F5>', ':!./wine.sh build_win95/testDdraw/test2.exe<CR>')


Set compile_commands.json path for clangd
=========================================
- Create .clangd file:

CompileFlags:
  CompilationDatabase: build_win95

TODO
====
- Fullscreen page flipping [OK]
- Fullscreen toggle [FAIL]
- Make window unresizeable [OK]
- Add keys to zoom in and zoom out (in multiples of base screen size)
- Split git repo
- Change palette so we don't use reserved colors (and change swatch)

